const data=window.ELKHORN_WEEKLY;const root=document.querySelector('[data-weekly]');if(root&&data){document.querySelector('[data-weekly-updated]').textContent=data.updatedLabel;document.querySelector('[data-weekly-title]').textContent=data.headline;document.querySelector('[data-weekly-intro]').textContent=data.introduction;root.innerHTML=data.sections.map(item=>`<article class="weekly-card"><p class="eyebrow">${item.kicker}</p><h2>${item.title}</h2><p>${item.body}</p><p class="weekly-status">${item.status}</p><a class="text-link" href="${item.link}" target="_blank" rel="noopener noreferrer">${item.linkLabel} <span aria-hidden="true">↗</span></a></article>`).join('');const weather=document.querySelector('[data-weather]');weather.querySelector('.eyebrow').textContent=data.weather.label;weather.querySelector('h2').textContent=data.weather.title;weather.querySelector('.weather-copy').textContent=data.weather.body;const link=weather.querySelector('a');link.href=data.weather.link;link.firstChild.textContent=data.weather.linkLabel+' ';}
